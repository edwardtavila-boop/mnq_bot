# Post-Mortem — 2fbc62b0845043f590017e3d16a9bdd4 (LOSS)

Auto-generated from journal on close. Consumes only `firm/templates/*`
markdown; the in-progress `desktop_app/firm/*` Python is untouched.

## Thesis

SHORT entry on 2026-01-18T15:01:00+00:00 against a `chop` tape. Exit via `stop` at 20047.25 vs entry 20044.00.

## Evidence

- PnL: $-7.24 (commission $+0.74)
- Entry slippage: +1.0 ticks; exit slippage: +0.0 ticks.
- Regime at entry: `chop`; qty: 1.

## Red Team's primary dissent

- Short in chop regime — directional bias gate should have suppressed this signal. Check `allow_short` / trend_align_bars.

## Resolution

[ ] Fixed — how: adjust gauntlet; [x] Accepted as surviving risk — monitoring per falsification criteria; [ ] Overridden.

## Falsification

I abandon this setup if ANY of:

- In the `chop` / `short` bucket after the next 5 closed trades, win-rate must be ≥ 80% (current: 80.0% on n=5).
- Mean slippage for this bucket must be ≤ +1.5 ticks.
- Net PnL in this bucket across the next 5 trades must be positive; if not, this setup is retired.

## Monitoring

- First review: after the next 5 trades in the `chop` / `short` bucket.
- Success: bucket expectancy > $0 and exit slippage p95 ≤ +2.0 ticks.
- Failure: bucket net PnL < 0 across those 5 trades.

## Raw payload snapshot

```
order_id           = 2fbc62b0845043f590017e3d16a9bdd4
side               = short
entry_ts / exit_ts = 2026-01-18T15:01:00+00:00 → 2026-01-18T15:02:00+00:00
entry / exit price = 20044.00 → 20047.25
pnl_dollars        = -7.2400
commission         = +0.7400
exit_reason        = stop
regime             = chop
slippage_ticks     = +0.00
entry_slip_ticks   = +1.00
```
