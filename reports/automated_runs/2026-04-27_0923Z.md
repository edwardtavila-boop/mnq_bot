# Automated phase sweep — 2026-04-27 09:23 UTC

Triggered by scheduled task `automate-when-data-is-refreshed`. Edward not present.

## TL;DR

- pytest: **green** (1546 passed, 15 skipped, 50.67s)
- run_all_phases.py: **0 ok / 2 fail / 86 total** — hard-blocked at Phase 0 `firm_bridge`; every downstream stage skipped via the orchestrator's earlier-blocking-fail rule.
- Did NOT auto-fix per scheduled-task directive ("Do not auto-fix without context"). Fix scoped below for the next operator session.

## Path correction

Scheduled-task script referenced `C:/Users/edwar/projects/mnq_bot` and `.venv/bin/python`. Both stale post-2026-04-26 consolidation. Used canonical paths instead:

- repo: `C:/EvolutionaryTradingAlgo/mnq_bot/`
- python: `.venv/Scripts/python.exe` (Windows venv layout)

Recommend updating the scheduled task script to match.

## Failures

### Phase 0 · `firm_bridge` (rc=2, 0.1s)

```
NOT INTEGRATING: readiness probe failed.
- Firm code path: C:\Users\edwar\projects\firm
- Path exists: False
- Probe error: Firm code path does not exist
```

**Root cause:** [scripts/firm_bridge.py:68-69](mnq_bot/scripts/firm_bridge.py:68) hard-codes a candidate list:
```python
Path("C:/Users/edwar/projects/firm"),
Path("C:/Users/edwar/OneDrive/The_Firm/the_firm_complete/desktop_app/firm"),
```
Both predate the 2026-04-26 consolidation. Actual Firm code now lives at `C:/EvolutionaryTradingAlgo/firm/` (verified — `eta_engine/`, `scripts/`, `AGENTS.md` present). The `FIRM_CODE_PATH` env override mechanism exists ([firm_bridge.py:58](mnq_bot/scripts/firm_bridge.py:58),[177](mnq_bot/scripts/firm_bridge.py:177)) but is not set.

### Phase 0 · `live_sim` (rc=1, 2.3s)

```
mnq._shim_probe.ShimContractDriftError: contract drift:
  locked=bf88d73c1a4a4e2f live=28c2a0f436fca531.
Rerun `python scripts/firm_bridge.py --probe` to inspect,
then `--integrate` to regenerate the shim.
```

**Downstream of firm_bridge.** The pinned shim hash in `src/mnq/firm_runtime.py` was generated against an older Firm contract; live `firm.*` package has drifted. Cannot regenerate while the bridge can't locate the Firm code.

## Recommended fix (next operator session)

Pick **one** of:

1. **Update `firm_bridge.py` candidate list** — add `Path("C:/EvolutionaryTradingAlgo/firm")` to the list at `firm_bridge.py:68`. Touches the import-boundary helper, so wants a code review pass.
2. **Set `FIRM_CODE_PATH` env var** — `setx FIRM_CODE_PATH C:\EvolutionaryTradingAlgo\firm` (user scope) so all sessions and scheduled tasks see it. No code change.

Then rerun `python scripts/firm_bridge.py --integrate` to regenerate the shim. After that, `python scripts/run_all_phases.py` should clear Phase 0 and proceed through the 86-stage sweep.

## Artifacts written

- `reports/run_all_phases.md` — orchestrator ledger (existed; refreshed)
- `reports/firm_integration.md` / `.json` — bridge readiness probe (existed; refreshed)
- `reports/automated_runs/2026-04-27_0923Z.md` — this status report

## What the next session should pick up

1. Apply one of the two fixes above.
2. `python scripts/firm_bridge.py --integrate` (regenerates the shim against the new Firm contract — diff `src/mnq/firm_runtime.py` before committing).
3. `python scripts/run_all_phases.py` end-to-end (86 stages — the previous run was cut to 2).
4. Investigate why pytest passes (1546/15) but the orchestrator's bridge probe doesn't — the test suite is presumably stubbing the firm_runtime shim, which masks the contract drift in CI. Worth a quick audit of `tests/conftest.py` to confirm the stub is intentional and not hiding the same drift in unit tests.

No live trades, no remote pushes, no messages sent (per task directive).
