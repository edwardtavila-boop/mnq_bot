"""[REAL] Strategy spec -> concrete artifact generators.

Subpackages:
    pine         — emits Pine v6 source (visualization + TradingView sanity check)
    python_exec  — emits a `StrategyBase` subclass for the live executor
"""
